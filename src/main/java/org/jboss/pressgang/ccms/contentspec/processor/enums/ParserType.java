package org.jboss.pressgang.ccms.contentspec.processor.enums;

public enum ParserType {
    NONE, REFER_TO, PREREQUISITE, NEXT, PREVIOUS, TARGET, EXTERNAL_TARGET, EXTERNAL_CONTENT_SPEC, LINKLIST, INFO
}
